import React from 'react';

//display todays task based on sorting of dates 
//create new endpoint in BE to fetch all tasks but also sort them based on todays date - or could that be done on FE instead?


//adding tasks could be a separate component perhaps, so you could add tasks from both weekly and daily?
const Daily = () => {
  return (
    <div>
      <h1>Daily/Today page</h1>
    </div>
  );
}

export default Daily;
